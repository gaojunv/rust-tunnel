[package]
name = "rust-tunnel-wiki-serve"
version = "0.4.0"
edition = "2021"
description = "Tauri IPC backend for the wiki desktop app (skeleton: command surface not implemented yet)"
license = "MIT"

[lints]
workspace = true

[features]
default = ["search"]
search = ["rust-tunnel-wiki-core/search"]

[dependencies]
rust-tunnel-wiki-core = { path = "../wiki-core" }